<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.hoshinohome.model.dao.UserInfoDao">
	<select id="login" parameterType="UserInfo" resultType="string"> select * from userinfo where
		user_name=#{user_name} and user_password=#{user_password} </select>

	<insert id="register" parameterType="UserInfo"> insert into userinfo(user_id,user_name, user_password, user_address, user_favorite_place)
		values(#{user_id},#{user_name},#{user_password},#{user_address},#{user_favorite_place}) </insert>

</mapper>